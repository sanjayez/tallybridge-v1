# Windows Installer Workspace

This folder is the target home for the privilege-aware Windows installer.

The current MVP installer still lives in `scripts/install-bridge.ps1`. The production migration should move installer logic here without breaking the existing demo bootstrap until the new flow is validated.
