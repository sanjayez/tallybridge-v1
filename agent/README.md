# Agent Workspace

This folder is reserved for the production local agent.

The agent should become the center of gravity for the bridge:

- Tally discovery
- XML HTTP reads and writes
- loopback listener for TDL or TCP events
- local queue and idempotency
- cloud session and health heartbeat
