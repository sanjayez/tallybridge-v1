# Control Plane Workspace

This folder is the target home for the production control plane.

For now, the working MVP implementation still lives in `src/control-plane.js` at the repo root. During migration, code should move here in a way that preserves the existing demo flow until the new entrypoint is ready.
